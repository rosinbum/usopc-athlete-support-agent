{
  "name": "@usopc/evals",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "eval": "sst shell -- vitest run --config ls.vitest.config.ts",
    "eval:classifier": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/classifierAccuracy.eval.ts",
    "eval:escalation": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/escalation.eval.ts",
    "eval:groundedness": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/groundedness.eval.ts",
    "eval:correctness": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/correctness.eval.ts",
    "eval:trajectory": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/trajectory.eval.ts",
    "eval:citations": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/citations.eval.ts",
    "eval:disclaimers": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/disclaimers.eval.ts",
    "eval:semantic": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/semanticSimilarity.eval.ts",
    "eval:quality": "sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/qualityReview.eval.ts",
    "eval:quality:full": "QUALITY_REVIEW_SCOPE=full sst shell -- vitest run --config ls.vitest.config.ts src/evaluators/qualityReview.eval.ts",
    "seed-langsmith": "sst shell -- tsx src/scripts/seed-datasets.ts",
    "seed-langsmith:force": "sst shell -- tsx src/scripts/seed-datasets.ts --force",
    "quality:seed": "sst shell -- tsx src/scripts/seed-quality-review.ts",
    "quality:run": "sst shell -- tsx src/scripts/run-quality-review.ts",
    "quality:triage": "tsx src/scripts/triage.ts",
    "corpus:generate": "tsx src/scripts/generate-test-corpus.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@langchain/core": "^1.1.31",
    "@langchain/openai": "^1.2.12",
    "@usopc/core": "workspace:*",
    "@usopc/shared": "workspace:*",
    "agentevals": "^0.0.7",
    "langsmith": "^0.5.7",
    "openevals": "^0.1.4"
  },
  "devDependencies": {
    "sst": "4.1.10",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
