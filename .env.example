# Database (local dev via docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/usopc_athlete_support

# LangSmith project name (optional, defaults to usopc-athlete-support)
# LANGCHAIN_PROJECT=usopc-athlete-support

# App
NODE_ENV=development
LOG_LEVEL=debug

# ---
# All API keys are managed by SST secrets (not needed in .env):
#   sst secret set AnthropicApiKey <key>
#   sst secret set OpenaiApiKey <key>
#   sst secret set TavilyApiKey <key>
#   sst secret set LangchainApiKey <key>    # Optional, for LangSmith tracing
#   sst secret set SlackBotToken <key>
#   sst secret set SlackSigningSecret <key>
