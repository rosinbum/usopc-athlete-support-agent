config:
  gcp:project: usopc-athlete-support-prod
  gcp:region: us-central1
  usopc:environment: production
  usopc:dbTier: db-custom-4-15360
  usopc:minInstances: "1"
  usopc:maxInstances: "10"
  # Set via: pulumi config set --secret usopc:dbPassword <password> --stack production
  # usopc:dbPassword: <encrypted>
